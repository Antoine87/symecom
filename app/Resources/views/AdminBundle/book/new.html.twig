{% extends 'base.html.twig' %}

{% block body %}
<div class="col-md-8 col-md-offset-2">
    <h1>Book creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-sl btn-primary" type="submit" value="Create" >
            Create
        </button>
    {{ form_end(form) }}

    <ul class="list-inline">
        <li>
            <a class="btn btn-sm btn-primary" href="{{ path('livre_index') }}">Back to the list</a>
        </li>
    </ul>
</div>
{% endblock %}
